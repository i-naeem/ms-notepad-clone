import { DropdownItem } from '../../styles/DropdownItem';
import { Dropdown } from '../../styles/Dropdown';

export const FormatNavbarItem = (props) => {
  return (
    <Dropdown label="Format">
      <DropdownItem label="Word Wrap" />
    </Dropdown>
  );
};
