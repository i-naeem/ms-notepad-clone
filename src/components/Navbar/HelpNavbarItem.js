import { DropdownItem } from '../../styles/DropdownItem';
import { Dropdown } from '../../styles/Dropdown';

export const HelpNavbarItem = (props) => {
  return (
    <Dropdown label="Help">
      <DropdownItem label="About" />
    </Dropdown>
  );
};
